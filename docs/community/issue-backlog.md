# Starter Issue Backlog

Use these drafts when seeding the public issue tracker after `v0.1.0`.

## Good First Issue

1. Add a second minimal example that demonstrates `stale_policy: warn`.
   Labels: `good first issue`, `docs`, `examples`

2. Add a troubleshooting note for missing `rg` on Windows, macOS, and Linux.
   Labels: `good first issue`, `docs`

3. Add a sample JSON report fixture to the documentation.
   Labels: `good first issue`, `docs`, `reporting`

4. Add tests for malformed `last_verified_commit` values.
   Labels: `good first issue`, `tests`, `validation`

5. Improve `docs/frontmatter.md` with a compact field reference table.
   Labels: `good first issue`, `docs`

## Help Wanted

1. Design a Python adapter preview that extracts stable function/class
   fingerprints without adding heavyweight dependencies.
   Labels: `help wanted`, `adapters`, `python`

2. Add a GitHub Actions integration guide for running `driftkb validate` in PR
   workflows.
   Labels: `help wanted`, `ci`, `docs`

3. Explore a portable static graph cache generator interface outside DriftKB
   core.
   Labels: `help wanted`, `graph`, `design`
